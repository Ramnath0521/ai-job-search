# Copy this file to .env and fill in real values. .env is gitignored — never
# commit real keys. Bun auto-loads .env from the repo root, so any portal-skill
# CLI invoked with `bun run ...` from here picks these up automatically.

# firecrawl-search skill (.agents/skills/firecrawl-search) — get a key at
# https://www.firecrawl.dev/app/api-keys (free tier: 1,000 credits/month, no card required)
FIRECRAWL_API_KEY=
